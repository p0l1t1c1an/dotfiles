{
    "layer": "top", // Waybar at top layer
    // "position": "bottom", // Waybar position (top|bottom|left|right)
    "height": 20, // Waybar height (to be removed for auto height)
    // "width": 1280, // Waybar width
    // Choose the order of the modules
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": ["custom/mem", "custom/drive", "custom/temp", "cpu", "custom/volume", "custom/backlight", "custom/bat0", "clock"],
    "sway/window": {
	"format": "{}",
    	"max-length": 50
    },
    "sway/mode": {
	"format": "{}"
    },
    "clock": {
        "tooltip-format": "{:%Y-%m-%d | %H:%M}",
        "format-alt": "{:%Y-%m-%d}"
    },
    "cpu": {
        "format": "{usage}%",
        "tooltip": true,
	"interval": 5
    },
    "custom/mem": {
        "format": "{} Mb",
	"exec": "freecolor -t -o -m | grep Total | cut -d '(' -f2 | tr -d ' '",
	"interval": 5
    }, 
    "custom/drive": {
        "format": "{} Gb",
	"exec": "zpool list | cut -d 'G' -f3 | tr -d ' ' | grep '[0-9]'",
	"interval": 5
    },
    "custom/temp": {
        "format": "{}°C",
        "exec": "sysctl -n hw.acpi.thermal.tz0.temperature | tr -d C",
	"interval": 5
    },
    "custom/backlight": {
        "format": "{}%",
	"exec": "sysctl -n hw.acpi.video.lcd0.brightness",
	"interval": 5 
    },
    "custom/volume": {
        "format": "{}%",
        "exec": "mixer vol | cut -d ':' -f2",
        "interval": 5
    },
    "custom/bat0": {
        "format": "{}",
        "exec": "acpiconf -i 0 | grep -E -o '[0-9][0-9]?%'",
	"interval": 5
    }
}
